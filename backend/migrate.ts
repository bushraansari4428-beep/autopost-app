import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); async function main() { const sources = await prisma.source.findMany({where: {platform: 'MEGA_CLOUD'}}); for(const s of sources) { const uuid = s.url.replace('cloud://', ''); const page = await prisma.facebookPage.findUnique({where: {id: uuid}}); if(page) { await prisma.source.update({where: {id: s.id}, data: {url: 'cloud://' + page.pageId}}); console.log('Migrated', page.name); } } } main();  
